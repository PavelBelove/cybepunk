from core.character_data.stats.presets.rocker import rocker_presets
from core.character_data.stats.presets.fixer import fixer_presets
from core.character_data.stats.presets.solo import solo_presets
from core.character_data.stats.presets.netrunner import netrunner_presets
from core.character_data.stats.presets.nomand import nomand_presets
from core.character_data.stats.presets.tech import tech_presets


STATS_PRESETS = {
    'ROCKER': rocker_presets,
    'FIXER': fixer_presets,
    'SOLO': solo_presets,
    'NOMAND': nomand_presets,
    'NETRUNNER': netrunner_presets,
    'TECH': tech_presets,
}
