from character_data.stats.presets.rocker import rocker_presets
from character_data.stats.presets.fixer import fixer_presets

stat_presets = {
    'ROCKER': rocker_presets,
    'FIXER': fixer_presets
}
